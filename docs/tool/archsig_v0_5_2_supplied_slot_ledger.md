# ArchSig v0.5.2 supplied slot 台帳

> ※residual 供給(supp)と coefficient slot は Issue #3820 で返済済み(residual は導出、係数は MeasurementProfile 帰属)。repair primitives(C⁰ / δ⁰、resL/resR)と zeroOverlapRef は Issue #3821 で返済済み(repair cochain は compare が測定記録から導出、primitives slot は v0.5.6 で撤去)。
>
> **負債告知(2026-07-26、#3822 で SAGA 系返済完了)**: 本台帳が記述してきた RepairPlan 系
> slot(repair primitives、faithfulness data、component-local cocycle、true sheaf certificate、
> gluing data、comparison data、presentation packet、saga-grounding packet)は、入力トライアド
> (ArchMap=観測 / LawPolicy=法・方程式 / ArchSig=計算)に帰属しない第三入力チャネルであり、
> #3820(residual 導出化)・#3821(repair cochain 導出化)・#3822(残る供給 slot の沈黙化)で
> 全行が処分された。現行 RepairPlan(v0.5.7)は選択複体(charts / overlaps / tripleOverlaps /
> enumerationComplete)だけを宣言する。残る負債は refactor morphism / refinement data の 2 行で、
> 処分は #3817 本体の残スコープである。観測行と law equation grounded surface /
> diagnostic ceiling / cost model の行は二系統に帰属する正規入力であり負債ではない。
> 本台帳は歴史記録+残 2 行の管理表であり、新規 slot 追加の根拠として引用してはならない。
> 台帳に無い供給入力面も負債でないことを意味しない。

この台帳は ArchSig v0.5.2 R1 で受け入れた供給 slot の記述表(歴史記録)である。処分列が示すとおり SAGA 系の行は #3820–#3822 で返済済みであり、現行実装(v0.5.7)はこれらの供給を受理しない。各行の validator / fixture 列は受け入れ当時の記述であり、削除済み fixture は git 履歴に残る。現行挙動を記述するのは正規入力行(観測 / law equation grounded surface / diagnostic ceiling / cost model)と derived residual class agreement 行、および残 2 行(refactor morphism / refinement data)だけである。

## R13 witness 帰属

- 3-chart の circle nerve は `circleSimplex` / `circleNext`、係数 `Z/(2)` に対応する Lean witness として扱う。
- 2頂点・逆向き2辺の circle nerve は本文の例9.2 / 付録 B.9 の worked example を固定する補助 fixture `tests/fixtures/ag_measurement/circle_nerve_two_vertex_body_v052.json` として扱う。この形を Lean proved witness として帰属させない。

| supplied 成分 | 理論典拠 | artifact の置き場 | validator | fixture | 解禁される語彙 | 実装段 |
| --- | --- | --- | --- | --- | --- | --- |
| 観測(atoms / contexts / covers) | Current tooling contract / 第X部 (A) | `archmap/v0.5.2` | ArchMap R1-R3 | `tests/fixtures/ag_measurement/archmap_v2.json` | 層 B の生値 | PR-1済 |
| repair primitives(導出化済み: supp は #3820 で選択 cover の cech sectionValue 比較+法曲面 witness 束縛から、C⁰ repair cochain は #3821 で head/repaired の測定記録の差分から、いずれも ArchSig が導出する。primitives slot 自体は v0.5.6 で撤去) | Current tooling contract / 定義3.1 (B) | `archsig-repair-plan/v0.5.6`(導出) | 供給検査なし(導出+fail-closed fault) | `tests/fixtures/ag_measurement/repair_plan_complete_support.json` | 境界所属 | 導出化済(#3820 residual / #3821 repair cochain) |
| derived residual class agreement(compare が 2 run の residual derivation から delta = r_base XOR r_head を作り δ⁰h = delta を解く。comparability gate と cover / law surface / chart 集合の相等検査つき) | Current tooling contract / 第X部 定義3.4・定理4.4 | `archsig-comparison-report/v0.5.5` の `residualClassAgreement`(導出。供給 slot ではない) | comparability level(identical / verdict-row)+ derivation provenance 相等 + 複体一致。不一致は named reason で fail-closed | `cli_compare_derives_repair_cochain_when_residual_change_is_a_boundary` | 2 run の residual class 一致(cohomologous / not_cohomologous)の記録。修理成功判定ではない | #3821 |
| faithfulness data(Q / faithfulness law。zeroOverlapRef は情報量ゼロのため v0.5.6 で撤去し、零 residual の component 帰属は trueSheafCertificate.memberCharts で決める) | Current tooling contract / 定義4.6 | RepairPlan `faithfulness.mode=supplied` | shape 検査+評価時に Q(r) supportVariables を導出 residual と突合(不一致は class 認証を落とす) | `tests/fixtures/ag_measurement/repair_plan_supplied_faithfulness.json`(供給時代の形は git 履歴) | complete-support 以外の肯定的大域整合 | **#3822 で沈黙化(slot 撤去)** |
| component-local cocycle(係数は Issue #3820 で MeasurementProfile の宣言へ帰属し、RepairPlan の coefficient slot は撤去済み) | Current tooling contract / 第X部 定義4.2 / 補題4.3 | residual support component の `complex.tripleOverlaps`  | 係数は profile 宣言(F2)を gate として読む。cocycle は導出 residual の triple パリティで検査する。triple は一意IDを持つ三 chart・三 edge の実 triangle として検査する。`complex.enumerationComplete=true` の component に triple が無い場合だけ `automatic-c2-zero` 認証を出力し、この行の `cocycle.checked` は false になる(検査が走らないため) | `tests/fixtures/ag_measurement/repair_plan_component_aware_one_cent.json`(現行は選択複体のみ。供給時代の形は git 履歴) | 層 C の class 語彙 | **#3822 で導出化(cocycle 検査は導出 residual の triple parity)** |
| true sheaf certificate | Current tooling contract / 第X部 定義8.1 | RepairPlan `trueSheafCertificate` | cover membership + `globalCondition=assumed` の assumption 記録。residual class 認証では certificate の cover / memberCharts が residual support component と完全一致し、class row の `dependsOnAssumptions` がこの assumptionId を参照する | `tests/fixtures/ag_measurement/repair_plan_component_aware_one_cent.json`(現行は選択複体のみ。供給時代の形は git 履歴) | 第X部 定理8.2のactual global gluing 読み | **#3822 で沈黙化(slot 撤去。global sheaf 読みは語彙ごと沈黙)** |
| gluing data | Current tooling contract / 第X部 定義8.1 / 定理8.2 | RepairPlan `gluingData` | declared overlap subset + sectionRef の一対一対応検査。residual class 認証では subset が residual support component の overlap 集合と完全一致する。sectionRef は非空かつ相異なることだけを要求する author の不透明ラベルであり、ArchSig は解決しない | `tests/fixtures/ag_measurement/repair_plan_component_aware_one_cent.json`(現行は選択複体のみ。供給時代の形は git 履歴) | grounded gluing refs | **#3822 で沈黙化(slot 撤去)** |
| comparison data(incidence bridge + H¹ comparison) | Current tooling contract / 定義7.1 / 定理6.4 | RepairPlan `comparison` + `h1-comparison-data/v0.5.2` | chart-indexed 集合一致または canonical explicit refs (`complex:repair` / `complex:cech` / `comparison:cochain-map`) + source/target fingerprint + target cochain support の `Z¹/B¹` 再計算。explicit は `cochainMap` の次数0/1/2有限基底写像表と次数2 `zeroImage` から、次数1両側逆・差保存・次数2零保存・微分可換を再計算し、宣言booleanは受けない | `tests/fixtures/ag_measurement/repair_plan_comparison.json`(供給時代の形は git 履歴) | 層 D の転送 invariant | **#3822 で沈黙化(slot 撤去。run 対の読みは compare の導出 residualClassAgreement へ)** |
| presentation packet(semantic / equation generators、relation 行列、generator map、restriction、equation lift atlas) | Current tooling contract / 第X部 定義10.1 | RepairPlan `comparison.h1ComparisonData.presentation`(`kind: presentation-generated`) | cell 集合が複体を完全被覆、generator 名の非空・相異、行列 shape。`coefficientRing` が `f2` なら F₂ 上、`integers` なら有限生成可換群として `im(R_V)=ker(χ̃_V)` と `im(χ̃_V)=Q_E(V)`(後者は列 Hermite 正規形で決定)、restriction naturality、atlas の chart / overlap 完全被覆。equation generator は法曲面の witness variable / skeleton simplex へ解決するか、`unbound-equation:` で対応物不在を明示すること。packet validator は埋め込み canonical RepairPlan から generated evidence を再計算し `inputDigests.repairPlan.sha256` と照合する(residual supports は packet 自身の residual-derivation invariant を参照し、RepairPlan からは再計算されない) | `tests/fixtures/ag_measurement/repair_plan_presentation_generated_circle.json` / `..._circle_integers.json`(いずれも #3822 で削除、git 履歴に残る。導出 residual はカット実現可能性により例10.2 の r=(1,0,0,0) を表現できない)。例10.2 の形式化の家は `Formal/AG/SemanticRepair/Saga/CircleWitness.lean`(FiniteModel 一式)だが、これは法 2 版(`M_sem=ℤ[σ]/(2σ)`、`Q_E=ℤ/(2)`)であり、#3805 由来の five-torsion 符号判別と格子相等の負例は Lean 側では未移植である(integer_lattice モジュールは #3822 で撤去、検出力の記録は git 履歴と当時の報告) | 層 D の presentation-generated `χ / Φ / κ` と quotient-level class transfer | **#3822 で沈黙化(slot 撤去。定理5.1 有限 instantiation の家は Lean)** |
| law equation grounded surface | Current tooling contract / 定義5.1 / 定義11.3 | `law-equation-surface/v0.5.2` (`skeleton` / `defectSources[].holdsCriterion` / `quotientSheafCondition`) | Stage 3 shape + law/axis/predicate resolution; analyze 時の atom/chart/cover 解決; single-context-theorem の単元性検査 | CLI Stage 3 正系 fixture + `law_surface_ag_v052.json` runtime path | 層 E の結論群を後続 evaluator に供給する grounded surface | PR-3実装済 |
| saga-grounding / 結論 packet | Current tooling contract / 第X部 定義5.1 / 定理7.5 / 定義8.1 / 定理8.2 / 第III部 系11.5 | RepairPlan `grounding.kind=saga-grounding` + `archsig-saga-conclusions/v0.5.2` | grounding/profile/surface/cover の参照整合、F₂ additive、有限上限、holdsCriterion 生値、witnessVariables / forbiddenSupportGenerators からの quotient 生成、nested packet validator、detector | `cli_analyze_practical_grounded_emits_defect_quotient_invariant`(存続部の defect-quotient を検査。供給時代の split-packet テストは #3822 で削除) | lawDependent 3結論 / lawIndependent 6結論、degree-zero寄与、chart detector、生成商 provenance。higher coherence は原則8.4により分離し結論に数えない | **#3822 で沈黙化(結論 packet 撤去。per-chart holdsCriterion 読みと生成商は観測+法から導出する defect-quotient invariant として存続)** |
| diagnostic ceiling / policy profileRef | Current tooling contract | `measurement-profile/v0.5.2.diagnosticCeiling` + `policies[].profileRef` | registered ceiling stage、policy profile ID 解決、未到達時の `silence_by_design`、site/cover validator | `measurement_profile_*` CLI negative/positive locks | 選択診断段の boundary contract | PR-3実装済 |
| cost model(Lipschitz L + harmonic resolution) | Current tooling contract / 第VIII部 系8.7 | `measurement-profile/v0.5.2` analytic 宣言 | innerProduct weights + costModel kind/L/resolution、assumption ledger 記録 | `tests/fixtures/ag_measurement/measurement_profile_harmonic_debt.json` | harmonicDebtNorm、cost model供給時のみrepair下界行、未供給時は invariant / analytic reading の `whatNext` を一致させた typed silence | PR-5実装済 |
| refactor morphism(site 射 + law / coefficient / witness 互換) | Current tooling contract / 第VIII部 定理7.3 | `refactor-morphism/v0.5.2` | site / cover / law / coefficient / finite witness compatibility の fail-closed 検査 | `tests/fixtures/ag_measurement/refactor_morphism.json` | validated artifact + witness による analytic verdict transport | PR-6実装済 |
| refinement data(粗→細) | Current tooling contract / 命題4.10 | `refinement-comparison/v0.5.2` | coarse-to-fine direction + `zeroTransport.checked` + coarse/fine `complexFingerprint` と base/head の `inputDigests.siteCoverDigest.sha256` の突合 | `tests/fixtures/ag_measurement/refinement_comparison.json`（`cli_refinement_fixture_binds_to_generated_run_fingerprints`でrun-bound lock） | compare の coarse-zero → fine-zero 引き継ぎ(`CLASS_ZERO_TRANSPORTED_UNDER_CHECKED_REFINEMENT`) | Issue #3377是正済 |

## 台帳外入力の扱い

結論相当の値、商水準の同一視、zero class、law の成立宣言を supplied slot に追加しない。未知フィールド、結論語を含む supplied block、または台帳に対応しない supplied 成分は fail-closed とする。供給されていない slot に依存する evaluator は verdict 行を立てず、PRD の Failure Contract に従う `silence_by_design` 行を生成する。

## 検証責務

この文書は入力台帳であり、実装の完了根拠ではない。各行の validator の assertion と fixture の内容は、対応する PR の tests と生成 packet を実読して確認する。slot の列挙完全性と global sheaf condition は `assumed_by: author` の assumption ledger 行として残す。
