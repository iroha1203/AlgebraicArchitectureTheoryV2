# G-aat-quality-surface-08 — intrinsic law-response circuit descent

- `id`: `G-aat-quality-surface-08`
- `status`: `draft`
- `research mode`: `target-theorem`
- `source note`: [docs/note/aat_intrinsic_law_response_circuit_descent.md](../../docs/note/aat_intrinsic_law_response_circuit_descent.md)
- `predecessor / relation`: G-07のlaw-generated `I/I^2`、generated cover、first-order descent engineを使う。G-03は一般generated nerveの並行研究、G-04はideal-power higher stages、nonabelian / higher / stacky / universalityを担当する。
- `research aim`: Atom / law / first-order ArchitectureOperationからchartwise conormal responseとcompatible law-response mapsを生成し、局所repair不能性のsupport-minimal circuitと、局所repairの大域化を判定するresponse-kernel値Čech classを同じoperation geometryから構成する。
- `core tension`: `I/I^2`を持つだけではallowed operationがどのlaw responseを生成できるかは分からない。semantic operationからchartwise`J_W : E_A(W) -> Hom(I_W/I_W^2, O_Y(W))`とrestriction-compatibleな`alpha_e : E_A -> O_Y`を生成し、局所span failureと大域descent failureを順に判定できるか。
- `rival`: matroid / hyperplane arrangement、Orlik–Terao / syzygy、classical deformation complex、cellular sheaf、generic torsor descent。既知の骨格の再命名ではなく、AAT入力からのfunctorial response生成、circuit / descentの統合、atom-generated witnessで差分を作る。
- `claim boundary`: affine chart spectra`Y_W = Spec(A_W/I_W)`を持つfinite/small atom-generated ringed AAT geometry、generated finite cover、finite law label型`L`、`P subset L`、`t in L \ P`、compatible labeled conormal map、field-valued geometric fibers、finite locally free allowed-operation sheaf、protected response map`A_P`のconstant rankとkernel base-changeが証明された対象、cover-relative additive Čech classを扱う。
- `capability categories`: intrinsic-operation-response、conormal-Jacobian、local-circuit-certificate、response-kernel-descent、circuit-locus、presentation-naturality、atom-generated-witness。
- `threshold policy`: draft中はSCOREを設定しない。G-07のsemantic representation、operation–derivation comparison、finite witness prototype、文献監査を確認してからactive化を判断する。
- `portfolio constraint`: target化する場合、有限線形repair alternative、intrinsic Jacobian、semantic operation comparison、`ker(s_t)`値descent、local-circuit / pure-descent witness対をすべて必須面とする。Torや不等式だけで完了しない。
- `phase boundary criteria`: draft段階では、operationのsemantic定義、固定statement、material premise、finite witness、既知理論との差が確定した時点でactive化を人間に提案する。target化後は固定statementの全artifactと独立最終査読を満たした場合だけ`target-theorem-proved`とする。
- `reward rubric`: `not-applicable (draft target-theorem candidate)`。
- `dullness filter`: linear algebra lemmaだけ、supplied `J`、supplied naturality、supplied effectivity、circuit名だけのspan theorem、G-07 torsor theoremの係数名替え、loopをTorが検出するclaim、external AG exampleだけ、semantic operationをderivationと定義して得る自明な同値を弾く。
- `frontier`: allowed derivation sheaf、square-zero operation、conormal dual、law-response matroid、support-minimal circuit、Fitting / determinantal locus、response polynomial ring、degree-one Tor、Čech torsor class、two-term repair complex、no-free-repair inequality、atom-generated shear witness、labeled presentation naturality。

- `target theorem candidate`: **Intrinsic Law-Response Circuit–Descent Theorem**。G-07から生成された`C = I/I^2`と、独立に定義されたfirst-order ArchitectureOperationからoperation sheaf`E_A`、chartwise Jacobian`J_W`、restriction-compatibleなlaw-response maps`alpha_e : E_A -> O_Y`を生成する。`P subset L`と`t in L \ P`に対して`K_P = ker(A_P)`、`s_t : K_P -> O_Y`、`K = ker(s_t)`、`Q = coker(s_t)`を構成し、次を証明する。
  1. 各fiberでnormalized repairの存在は`alpha_t`が`span(alpha_P)`の外にあることと同値で、failureは`t`を含むsupport-minimal circuitを持つ。
  2. `E_A`がfinite locally freeで、`A_P`がlocally constant rankを持ち、kernel formationがresidue-field base changeと可換であるとき、`Supp(Q)`のunderlying setはfiber circuit locusに一致する。
  3. selected cover上にexplicit chartwise normalized repairsが存在するとき、その差はchoice-independentな`omega_(P,t) : CechH1(U, K)`を定め、`omega_(P,t) = 0`とactual global normalized ArchitectureOperationの存在が同値である。
  4. 同一ambient siteと共通ambient operation schemaから各lawful locusへcanonicalにbase changeし、law dataを変えたlocal circuit witnessと、全fiberおよび各selected chartでlocal repair可能だが`omega_(P,t) != 0`となるpure-descent witnessを構成する。
  5. cover、`P,t`、operation sheaf、labeled conormal data、response squareを保つ同型に対するcircuit locus、repair existence、response-kernel classのnaturalityを証明する。unit rescalingはnormalization transportを明示した別statementとする。
- `target theorem boundary`: primary Lean theoremはfinite linear circuit、intrinsic Jacobian、generated restriction naturality、cover-relative `K`値descent、finite witness対を対象とする。nonloop response Torはsecondary artifactとし、graded internal degree、一般hypercohomology、Chow class、cotangent complex、arbitrary presentation changeは初期completionに含めない。Lean置き場所候補は`research-lean/ResearchLean/AG/QualitySurface/IntrinsicLawResponseCircuitDescent/`。
- `target proof artifacts`: `LinearRepair.lean`、`SupportMinimalCircuit.lean`、`ConormalJacobian.lean`、`SemanticFirstOrderOperation.lean`、`RestrictionNaturality.lean`、`CircuitLocus.lean`、`ResponseKernelCechDescent.lean`、`PresentationNaturality.lean`、`LocalCircuitWitness.lean`、`PureDescentWitness.lean`、`MainTheorem.lean`、統合入口`IntrinsicLawResponseCircuitDescent.lean`、main declaration`intrinsicLawResponseCircuitDescent_package`、report `research/reports/G-aat-quality-surface-08.md`。Tor bridgeはprimary artifact完了後のsecondary fileとする。
- `target proof strategy`: L0 finite linear repair / circuit -> J0 semantic operation -> J1 operation–derivation equivalence -> J2 derivation to conormal response -> J3 restriction naturality -> C0 fiber circuit locus -> D0 response-kernel cocycle -> D1 choice independence -> D2 zero iff global operation -> E0 witness pair。
- `target theorem completion criteria`: target化時に確定する。最低条件は、primary equivalenceとsupport minimality、chartwise`J_W`と`alpha_e`のinput-generated construction、semantic operation comparison、generated restriction naturality、support–fiber circuit locus一致、actual global operationの双方向判定、presentation naturality、共通ambient operation schemaからbase changeしたwitness対がsorryなしでLeanに受理され、material premise discharge、axiom / placeholder audit、route-integrity audit、report / tracking Issue同期、final_review_packetを揃え、4本の`$math-lean-review`がすべて`No major findings`であること。
- `target premise discharge policy`: field、finite labels、`P subset L`、`t in L \ P`、finite/small ringed geometryは入力として残せる。operation module/sheaf構造、operation restriction、operation–derivation comparison、conormal class compatibility、`J_W`のwell-definedness、response naturality、`A_P` kernel base-change、support–fiber comparison、Čech instance、actual gluing、semantic preservation / reflection、presentation naturality、witness nonvacuityはtarget completionまでにtheoremまたはfinite constructionで放電する。
- `target material premise ledger candidate`:
  - `ringed AAT geometry / labels / P,t`: `ambient-boundary`。points、stalks、residue fields、generated cover、`t in L \ P`を固定し、repair existenceを含めない。
  - `chartwise local repairs`: `direction-hypothesis`。selected cover上のexplicit familyとし、global compatibilityを含めない。
  - `operation module/sheaf / restriction / derivation representation`: `discharge-required`。semantic operationから構成し、derivationとの同値をclass membershipへ入れない。
  - `conormal response / A_P kernel base-change / support comparison`: `discharge-required`。law witnessとrestrictionから証明し、circuit locusをfieldへ入れない。
  - `Čech instance / actual gluing / semantic reflection`: `discharge-required`。class zeroまたはglobal operationをcertificate fieldへ入れない。
  - `presentation naturality / witness pair`: `discharge-required`。presentation同型はcover、`P,t`、operation、conormal、responseを運び、witness pairは同一ambient siteと共通operation schemaからbase changeして構成する。結論をwitness certificateへ埋め込まない。
- `target anti-weakening rule`: `E_A`、`J_W`、`alpha_e`のnaturality、local repair、effectivity、semantic equivalence、circuit minimality、global operationをargument / typeclass / structure field / certificate fieldとして供給してcompletionと呼ばない。objectwise dualを無証明でsheafとせず、fiberwise repair可能性をfixed-cover chartwise liftへ無証明で昇格しない。`I/I^2`とresponse kernelを同一視せず、G-07のsection liftをoperation liftと読み替えない。局所可解性と大域classを排他的な二分として述べない。
- `target failure policy`: operation–derivation comparisonまたはprimary zero-iff-global-operationに反例があればtarget statementを固定せずdraftへ戻す。kernel base-changeが一般には失敗する場合は証明可能なsite / operation classを明示して再査読する。新規性が既知理論の直接instanceに尽きる場合はtarget-theorem化せず、AAT applicationまたはsupport resultとして再分類する。
