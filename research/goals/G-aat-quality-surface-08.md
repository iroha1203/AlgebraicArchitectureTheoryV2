# G-aat-quality-surface-08 — intrinsic law-response circuit descent

- `id`: `G-aat-quality-surface-08`
- `status`: `active`
- `activated at`: `2026-07-12 JST`
- `priority`: `blocking`
- `research mode`: `target-theorem`
- `tracking issue`: [#3282](https://github.com/iroha1203/AlgebraicArchitectureTheoryV2/issues/3282)
- `source note`: [docs/note/aat_intrinsic_law_response_circuit_descent.md](../../docs/note/aat_intrinsic_law_response_circuit_descent.md)
- `predecessor / relation`: G-07のlaw-generated `I/I^2`、generated cover、first-order descent engineを使う。G-03は一般generated nerveの並行研究、G-04はideal-power higher stages、nonabelian / higher / stacky / universalityを担当する。
- `research aim`: Atom / law / first-order ArchitectureOperationからoperation tangent sheaf、conormal Jacobian、compatible law-response mapsを生成し、局所repair不能性のsupport-minimal circuitと局所repairの大域化を判定するresponse-kernel値Čech classを同じoperation geometryから構成する。effective finite measurement regimeではfiber response matrix、repair vector / minimal circuit、Čech cocycle / dual non-boundary witnessへ証明付きで落とし、AATのoperation geometryとArchSigの有限measurement shadowを接続する。
- `core tension`: `I/I^2`を持つだけではallowed operationがどのlaw responseを生成できるかは分からない。semantic operationからchartwise`J_W : E_A(W) -> Hom(I_W/I_W^2, O_Y(W))`とrestriction-compatibleな`alpha_e : E_A -> O_Y`を生成し、局所span failureと大域descent failureを順に判定できるか。
- `rival`: matroid / hyperplane arrangement、Orlik–Terao / syzygy、classical deformation complex、cellular sheaf、generic torsor descent。既知の骨格の再命名ではなく、AAT入力からのfunctorial response生成、circuit / descentの統合、atom-generated witnessで差分を作る。
- `claim boundary`: selected chartとoverlapがaffine spectra`Y_W = Spec(A_W/I_W)`で、generated chart mapをtyped Zariski localizationとして持つfinite atom-generated locally affine AAT geometry、generated finite cover、required`(lawIndex, atom)`からなるfinite generator label型`L`、`P subset L`、`t in L \ P`、law quotient前のambient algebra上のfinite primitive operation presentation、field-valued geometric fibers、finite locally free allowed-operation sheaf、locally split constant-rank protected response map、cover-relative additive Čech classを扱う。global ringed AAT objectを単一`Spec Γ(Y,O_Y)`と同一視しない。各generator labelは既存`violationCoordinate lawIndex atom`へprovenanceを持ち、その全体から`obstructionIdeal`とconormal generationを導く。chart evaluatorはambient derivationのlocalizationとlaw quotient mapの合成から構成し、labeled conormal class、operation restriction、kernel base change、support comparisonは入力fieldにせず生成・証明する。
- `capability categories`: operation-tangent-geometry、intrinsic-operation-response、conormal-Jacobian、local-circuit-certificate、two-stage-obstruction、response-kernel-descent、circuit-locus、presentation-naturality、atom-generated-witness、finite-measurement-shadow。
- `threshold policy`: SCOREは使わない。固定target statement、material premise ledger、anti-weakening rule、文献監査、実装phaseと停止規則をカード上で確定した後にactive化を人間へ提案する。typed presentationとBoolean-circle operation-response prototypeのLean構成はactive化前提にせず、active化後のPhase 0で検証する。
- `portfolio constraint`: target化する場合、有限線形repair alternative、intrinsic Jacobian、semantic operation comparison、`ker(s_t)`値descent、local-circuit / pure-descent witness対をすべて必須面とする。Torや不等式だけで完了しない。
- `phase boundary criteria`: draft段階では、square-zero liftとしてのsemantic operation、operation–derivation同値、input-generated conormal response、generic theoremの仮定role、既知理論との差、ArchSigへ渡す有限certificate contract、Phase 0–5の責務を固定した時点でactive化を人間に提案する。active化後はPhase 0のtyped foundationから順に進め、固定statementの全artifact、premise discharge、独立最終査読を満たした場合だけ`target-theorem-proved`とする。
- `reward rubric`: `not-applicable (active target-theorem; SCORE unused)`。
- `dullness filter`: linear algebra lemmaだけ、supplied `J`、supplied naturality、supplied effectivity、circuit名だけのspan theorem、G-07 torsor theoremの係数名替え、loopをTorが検出するclaim、external AG exampleだけ、semantic operationをderivationと定義して得る自明な同値を弾く。
- `frontier`: allowed derivation sheaf、square-zero operation、conormal dual、law-response matroid、support-minimal circuit、Fitting / determinantal locus、response polynomial ring、degree-one Tor、Čech torsor class、two-term repair complex、no-free-repair inequality、atom-generated shear witness、labeled presentation naturality。

- `target theorem candidate`: **Intrinsic Law-Response Circuit–Descent Theorem**。finite primitive operation label`Op`、selected`ArchitectureOperation` schema、law quotient前のambient derivation realization`rho_0 : Op -> Der_k(A_0,A_0)`を固定する。typed Zariski localizationから`rho_W`を構成し、law quotient mapと合成した`d_(I,W) = q_W o rho_W : Der_k(A_W,B_W)`に対応するsquare-zero algebra liftをsemantic first-order operation presentationとする。free module sheaf`O_Y^Op`からambient-to-quotient derivation sheafへのmapのsheaf-category imageとしてallowed-operation sheaf`E_A`を構成し、G-07から生成された`C = I/I^2`へのchartwise Jacobian`J_W`とrequired generator label`e = (lawIndex, atom)`の`violationCoordinate`由来のrestriction-compatible response`alpha_e : E_A -> O_Y`を生成する。ideal-preserving ambient directionsは入力にせず、conormal responseのkernelとして特徴づける。`P subset L`と`t in L \ P`に対して`K_P = ker(A_P)`、`s_t : K_P -> O_Y`、`K = ker(s_t)`、`Q = coker(s_t)`を構成し、次を証明する。
  1. 各fiberでnormalized repairの存在は`alpha_t`が`span(alpha_P)`の外にあることと同値で、failureは`t`を含むsupport-minimal circuitを持つ。
  2. `E_A`がfinite locally freeで、`A_P`がlocally split constant rankを持つとき、kernelがfinite locally freeでresidue-field base changeと可換であることを証明し、`Supp(Q)`のunderlying setがfiber circuit locusに一致することを導く。
  3. selected cover上にexplicit chartwise normalized repairsが存在するとき、その差はchoice-independentな`omega_(P,t) : CechH1(U, K)`を定め、`omega_(P,t) = 0`とglobal normalized `E_A`-sectionの存在が同値である。
  4. 同一ambient algebraと共通ambient operation presentationを固定し、同じambient derivationをlocalizeして各law quotient mapと合成し、lawful locusごとのoperation sheafを構成する。law dataを変えたlocal circuit witnessと、全fiberおよび各selected chartでlocal repair可能だが`omega_(P,t) != 0`となるpure-descent witnessを構成する。
  5. cover、`P,t`、operation sheaf、labeled conormal data、response squareを保つ同型に対するcircuit locus、repair existence、response-kernel classのnaturalityを証明する。unit rescalingはnormalization transportを明示した別statementとする。
  6. 一般の数学certificateとeffective finite measurement certificateを分ける。後者ではcomputable coefficient field、selected geometric fiber evaluation、有限基底付きresponse / Čech cochain module、boundary matrixを固定し、同じresponse mapからfiber response matrix、normalized repair vectorまたはsupport-minimal circuit coefficients、`K`値cocycle、coboundary image非所属を検証するdual functionalを生成する。
- `target theorem boundary`: primary Lean theoremはfinite linear circuit、intrinsic Jacobian、generated restriction naturality、locally affine global object上のcover-relative`K`値descent、finite witness対を対象とする。selected chartとoverlapはaffineとするが、global objectのaffinenessは要求しない。nonloop response Torはsecondary artifactとし、graded internal degree、一般hypercohomology、Chow class、cotangent complex、arbitrary presentation changeは初期completionに含めない。Lean置き場所候補は`research/lean/ResearchLean/AG/QualitySurface/IntrinsicLawResponseCircuitDescent/`。
- `target proof artifacts`: Phase 0専用の`ArchitectureOperationPresentation.lean`、`TypedLocalizationGeometry.lean`、`AmbientOperationLocalization.lean`、`SemanticOperationDerivation.lean`、`DerivationCoefficientSheaf.lean`、`OperationImageSheaf.lean`、`ConormalJacobian.lean`、`LabeledConormalGeneration.lean`、`ConormalResponseKernel.lean`、`BooleanCircleOperationResponsePrototype.lean`、および`LinearRepair.lean`、`SupportMinimalCircuit.lean`、`RestrictionNaturality.lean`、`CircuitLocus.lean`、`TwoStageRepairComplex.lean`、`ResponseImageSequence.lean`、`ResponseKernelCechDescent.lean`、`FiniteMeasurementChainMap.lean`、`FiniteMeasurementShadow.lean`、`PresentationNaturality.lean`、`LocalCircuitWitness.lean`、`PureDescentWitness.lean`、`CertifiedWitnessPair.lean`、`MainTheorem.lean`、統合入口`IntrinsicLawResponseCircuitDescent.lean`、main declaration`intrinsicLawResponseCircuitDescent_package`、report `research/reports/G-aat-quality-surface-08.md`。`.tmp/G08ApiProbe.lean`はAPI固定用の非aggregate scratch artifactとし、統合入口へimportしない。Tor bridgeはprimary artifact完了後のsecondary fileとする。
- `target proof strategy`: `L0 finite linear repair / circuit`は独立に実装可能だが、Phase 1 gateは`BC0 statement/API compatibility -> J0 typed ArchitectureOperation presentation -> J1a typed localization geometry -> J1b ambient derivation localization -> J2 quotient-valued derivation / square-zero adapter -> J3a derivation coefficient sheaf -> J3b operation image sheaf -> J4 conormal response / restriction -> J5a full response-kernel theorem -> J5b labeled conormal generation -> J6 typed Boolean-circle nonzero response`を順に要求する。`L0 + J6 -> C0 fiber circuit locus -> D0 image sequence / response-kernel cocycle -> D1 choice independence -> D2 zero iff global normalized E_A-section`まで進める。`E-pre witness construction / N0 naturality`と`M0 generic finite checker`はD2後に並行可能で、両者を`E-cert certified witness pair`で接続してからmain theoremへ進む。proof dependencyと実装gateを区別し、J6を通らずC0へ進まない。
- `target implementation phases`:
  - `Phase 0 — Semantic Operation Foundation`: `BC0`でlocally affine interpretation、reuse / replacement map、API probeを固定する。続いてselected`ArchitectureOperation`のstate / witness parametersとambient derivation realizationを結ぶtyped presentation、typed localization geometry、`rho_W`のcanonical localization、`q_W o rho_W`、square-zero adapter、derivation coefficient sheaf、operation image sheaf、conormal response、ideal-preserving方向とfull response kernelの同値、labeled conormal generation、G-08用typed Boolean-circle ring layer上の非零response prototypeを構成する。
  - `Phase 1 — Local Circuit and Support`: finite linear repair alternative、support-minimal circuit、loop分離、locally split constant-rankからのkernel base-change、`Supp(Q)`とfiber circuit locusの一致を証明する。
  - `Phase 2 — Response-Kernel Descent`: `R = image(s_t)`とshort exact sequence`0 -> K -> K_P -> R -> 0`を生成し、selected local repairsからdistinguished image sectionを構成する。local repair差、`K`値cocycle、choice independence、class zeroとglobal normalized`E_A`-sectionの双方向判定を証明する。
  - `Phase 3 — Witness and Naturality`: `E-pre`としてlocal-circuit / pure-descent witness対を構成し、`N0`としてstrict labeled presentation naturalityとunit rescaling transportを証明する。
  - `Phase 4 — Effective Measurement Shadow`: full response / Čech complexからcomputable coefficient field上のfinite measurement complexへのchain map、response matrix、repair / circuit、cocycle、dual non-boundary checkerとsoundnessを証明し、`E-pre`を`E-cert`へ接続する。full cochain module自体の有限次元性は要求しない。
  - `Phase 5 — Integration and Final Gate`: main package、report / tracking Issue / ledger同期、axiom / placeholder audit、4本の`$math-lean-review`を完了する。
- `target theorem completion criteria`: 全primary artifactがsorryなしで`ResearchLean`に受理され、対象declarationのstandard-axiom assertionとplaceholder auditがcleanであること。locally affine geometry、ambient derivation realizationからtyped localization、quotient-valued derivation、square-zero adapter、derivation coefficient sheaf、operation image sheafを構成し、ideal-preserving方向とfull conormal-response kernelの同値、required`(lawIndex, atom)` generator classesのgeneration、law witness由来conormal response、generated restriction naturality、local circuit theorem、regularity仮定からのkernel base change、support–fiber circuit locus一致、short exact image sequence、response-kernel Čech class、class zeroとglobal normalized`E_A`-sectionの双方向判定、presentation naturality、full-to-finite chain mapとfinite measurement certificate、共通ambient operation presentationを各law quotient mapと合成したwitness対を接続すること。下記ledgerを独立監査し、report / tracking Issue、final_review_packetを同期し、4本の`$math-lean-review`がすべて`No major findings`であること。
- `target premise discharge policy`: base field、finite required`(lawIndex, atom)` generator labels、`P subset L`、`t in L \ P`、typed locally affine Zariski geometry、selected`ArchitectureOperation` schema、ambient derivation realizationは入力として残せる。generator labelから`violationCoordinate`へのmapは既存law dataから固定し、任意の代表generatorは入力にしない。chartwise evaluator familyやrestriction compatibility、ideal-preservation certificate、labeled generation、image membership、global affinenessは入力にしない。`E_A`のfinite locally free性と`A_P`のlocally split constant rankはcircuit-locus theoremの明示的なdirection hypothesisとし、concrete witnessでは証明する。localization、quotient-valued composition、derivation coefficient sheaf、operation image sheaf、ideal-preserving方向のkernel characterization、labeled conormal classとgeneration、`J_W`、response naturality、kernel base change、support comparison、image sequence、Čech instance、actual gluing、presentation naturality、finite chain-map certificate、witness nonvacuityはcompletionまでに生成・証明する。
- `target material premise ledger`:
  - `ringed AAT geometry / generator labels / P,t`: `ambient-boundary`。locally affine global object、affine selected charts / overlaps、points、stalks、residue fields、generated cover、required`(lawIndex, atom)` generator label型、`t in L \ P`を固定し、任意のrepresentative generator、global affineness、repair existenceを含めない。
  - `chartwise local repairs`: `direction-hypothesis`。selected cover上のexplicit familyとし、global compatibilityを含めない。
  - `ambient Atom-operation presentation`: `ambient-boundary`。finite primitive operation labels、selected`ArchitectureOperation` schema、law quotient前のambient derivation realizationを固定するが、law-specific chart family、restriction compatibility、conormal response、repair、global sectionを含めない。
  - `operation module/sheaf / restriction / derivation representation`: `discharge-required`。ambient derivationのtyped localization、law quotient mapとの合成、sheaf-category image、mathlib`derivationToSquareZeroEquivLift`への接続から構成する。ideal-preserving方向はconormal-response kernelとして証明する。
  - `finite locally free E_A / locally split constant-rank A_P`: `direction-hypothesis`。fiber / support theoremにのみ使い、kernel base-changeやrepair existenceを含めない。finite witnessでは具体的に証明する。
  - `labeled conormal response / kernel base-change / support comparison`: `discharge-required`。各required`(lawIndex, atom)`を`violationCoordinate`へ送り、`obstructionIdeal`がrequired lawのatom-indexed rangeのspanであることから`I/I^2` classとgeneration theoremを導き、full responseとlabeled responsesを比較する。operation restrictionとFitting / finite-presentation APIからkernel base-changeとsupport comparisonを証明する。
  - `Čech instance / actual gluing / linearized semantic reflection`: `discharge-required`。`R = image(s_t)`、short exact image sequence、distinguished image sectionをselected local repairsから生成する。class zeroまたはglobal normalized`E_A`-sectionをcertificate fieldへ入れない。既存`ArchitectureOperation`への非線形reflectionは初期completionでは主張しない。
  - `finite measurement certificate / presentation naturality / witness pair`: `discharge-required`。full complexからfinite measurement complexへのchain map、effective coefficient field、selected fiber evaluation、有限basis、boundary matrixからmatrix、repair vector、minimal circuit、cocycle、dual non-boundary witnessを構成する。presentation同型はcover、`P,t`、operation、conormal、responseを運び、witness pairは同一ambient algebraと共通operation presentationをlocalizeして各law quotient mapと合成する。結論をcertificateへ埋め込まない。
- `target anti-weakening rule`: `E_A`、`J_W`、`alpha_e`のnaturality、labeled generation、local repair、image membership、effectivity、semantic equivalence、circuit minimality、global sectionをargument / typeclass / structure field / certificate fieldとして供給してcompletionと呼ばない。objectwise spanやdualを無証明でsheafとせず、fiberwise repair可能性をfixed-cover chartwise liftへ無証明で昇格しない。global objectを単一affine`Spec`へ縮めてpure-descent witnessを失わない。full cochain moduleを有限次元と仮定してmeasurement certificateを得ない。`I/I^2`とresponse kernelを同一視せず、G-07のsection liftをoperation liftと読み替えず、linearized`E_A`-sectionを既存`ArchitectureOperation`と同一視しない。局所可解性と大域classを排他的な二分として述べない。
- `target route integrity gate`: selected chart / cover / operation sheaf / coefficient / complex / realization / certificate / finite witnessは、GOALの入力data、canonical / free construction、universal property、finite construction、または独立査読済みpredecessor theoremから構成する。target conclusionを成立させるためだけのad hoc selection、空型・singleton・trivial relation・degenerate coverによるvacuity、片方向theoremの同値扱い、結論側のexactness / descent / effectivity / global coherence / obstruction vanishing / comparison / naturalityをstructure field / certificate field / typeclass / theorem argumentとして供給する経路、証明後にGOAL / reportを弱いtheoremへ合わせて読み替える経路をRejectとする。各completion artifactについてinput provenance、nonvacuity / adequacy、restriction / comparison lawの生成theorem、main proof termでのmaterial premise使用を`final_review_packet`で監査する。
- `target failure policy`: `BC0`でglobal objectのaffinenessとpure-descent非零witnessが両立しない解釈しか残らない場合、またはPhase 0でtyped presentation、typed localization geometry、operation image sheaf、full response-kernel theorem、labeled conormal generationのいずれかが構成不能、`q_W o rho_W`がrestriction-compatibleでない、G-08用typed Boolean-circle ring layerで非零responseが発火しない場合は後続phaseへ進まず、固定statement改訂を人間へ提案して再査読する。operation–derivation comparison、image sequenceからactual gluingへの接続、primary zero-iff-global-normalized-sectionに反例がある場合も同様とする。regularity仮定からkernel base-changeを導けない場合は証明可能なsite / operation classを明示して再査読する。新規性が既知理論の直接instanceに尽きる場合はtarget-theorem化せず、AAT applicationまたはsupport resultとして再分類する。
